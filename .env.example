# Production URL (NOT webhook-test): https://YOUR.app.n8n.cloud/webhook/godistyping-web
# webhook-test only works while listening in the n8n editor
# Local: cp .env.example .env  |  Vercel: Project Settings → Environment Variables
VITE_N8N_WEBHOOK_URL=
